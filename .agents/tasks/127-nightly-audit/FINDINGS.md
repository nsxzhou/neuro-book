# 夜审发现

状态只有人能改成 `confirmed` / `dismissed` / `fixed`，夜审只写 `open`。
摘要必须带函数名或代码片段——并发会话会让行号漂移。

| ID | 轮次 | 严重度 | 位置 | 摘要 | 状态 |
|---|---|---|---|---|---|
| F-001 | 2026-07-27-01 | medium | `scripts/deploy/product-start.mjs:14` | 顶层 `await prepareSystemAssets(...)` 排在 `process.once("SIGTERM")` 注册之前，资产同步全程 PID 1 无停止信号处理器；与同文件「必须把停止信号转发给真正的Nitro进程」注释矛盾。**第 2 轮已答：资产同步不是原子写入**，后果由 F-005 / F-006 承载 | open |
| F-002 | 2026-07-27-01 | low | `scripts/deploy/product-start.mjs:32` | 用 `process.once(signal, ...)` 而非 `process.on`，第二次 SIGTERM 不再转发给 Nitro，PID 1 走默认行为直接退出 | open |
| F-003 | 2026-07-27-01 | low | `packages/neuro-book-manager/src/health.ts:88` | `verifyNativeProduct` 轮询里只判 `child.exitCode !== null`，同函数 `finally`（第 109 行）却判 `exitCode === null && signalCode === null`；子进程死于信号时轮询看不见，空转满 120 秒并把「进程崩了」误报成 HTTP 超时 | open |
| F-004 | 2026-07-27-01 | low | `packages/neuro-book-manager/src/process.ts:6` | `RunOptions.stdio` 声明允许 `"pipe"`，但 `run()` 从不消费 `child.stdout/stderr`，输出超过管道缓冲即死锁且 Promise 永挂；当前零调用方使用，属埋雷。同文件 `runCapture()` 用 `close` 且有注释，做法正确 | open |
| F-006 | 2026-07-27-02 | **high** | `server/workspace-files/novel-workspace.ts:1474` | `writeUserSystemAssetsSyncState` 用裸 `fs.writeFile` 就地重写 sync state JSON（非原子），而 `readUserSystemAssetsSyncState` 的 catch 只兜 `ENOENT`、把 `JSON.parse` 的 SyntaxError 直接 throw。写入瞬间被打断后每次启动都在 `prepareSystemAssets` 抛错，`product-start.mjs` 顶层 await 无 try/catch → **Product 永久起不来且不自愈**，报错只有一段 JSON 解析栈。修法：写入走临时文件 + rename；解析失败按文件不存在处理（这是派生缓存，第 547 行分支证明可重建） | open |
| F-017 | 2026-07-27-08（**机制已于 09 轮纠正**） | **high** | `packages/neuro-book-manager/src/cli.ts:98` + `config.ts` `ensureStateFiles` 写出的 `.env` + `server/middleware/auth.ts` | **默认 Windows Portable 安装 = 绑所有网卡且完全无鉴权。** 逐级确认：①`auth.ts` 的 `if (!isAuthEnabled()) return;` 让守卫整体空操作；②`cli.ts:98` `authEnabled: options.auth ?? profile !== "windows-portable"` 使 Portable 默认关鉴权；③Portable 的 `.env` **不含 `HOST`**，Manager 原生启动路径也不设（全仓只有 Docker 侧设 `0.0.0.0`、health/verify 脚本设 `127.0.0.1`），而 Nitro 两个 preset 都是 `const host = process.env.NITRO_HOST \|\| process.env.HOST` → `undefined` → `listen()` 绑所有接口。**⚠️ 第 8 轮原写的机制「`config.yaml` 里 `host: 0.0.0.0` 生效」是错的，见 F-018——那个字段没有消费者，照它去改无效。** 修法：Portable 启动路径显式设 `HOST=127.0.0.1`，或 host 非环回时强制开鉴权。**结论由依赖源码逐级推出，未实测监听地址** | open |
| F-019 | 2026-07-27-10 | medium | `server/runtime/installation-paths.ts` 六个 `resolve*` 函数 | 每个都是 `startPath = process.cwd(), env = process.env` 的默认参数——**调用时求值**，于是每次调用重新解析根。这绕过了 `runtime-paths.ts` 自己写死的边界（`createRuntimePaths` 注释：「本函数不读取cwd或环境变量；生产、开发和测试Adapter必须在调用前决定两个根」，返回 `Object.freeze`，类型注释称「一次进程使用的**不可变**物理根集合」）。`boot-config.ts`、`database/config.ts` 都走这条路。**证据不是理论**：至少 4 个测试文件必须 `process.chdir` 才能控制解析（`test-workspace-fixture.ts:187`、`project-workspace-delete.test.ts:116/145`、`novel-chapter.test.ts:23`、`database/config.test.ts`）。生产侧影响未证实（非测试代码里 `process.chdir` 只有 `assets/.../scripts/profile.ts:6`，执行方式未确认），故取 medium | open |
| F-018 | 2026-07-27-09 | medium | `server/config/boot-config.ts` `BootConfig` 类型 + `packages/neuro-book-manager/src/config.ts` `ensureStateFiles` + `config.example.yaml` | `BootConfig` 声明了 `server?: {host?, port?}`，Manager 把它写进 `config.yaml`，`config.example.yaml` 也示范它——**但全仓没有任何读取点**（`boot-config.ts` 只导出 `resolveBootAuthEnabled` / `loadBootAuthEnabledSync`，只读 `auth.enabled`；`server/` 与 Manager 包 grep `.server` 均无消费点）。后果：察觉到 F-017 风险的用户把 `config.yaml` 的 host 改成 `127.0.0.1` 并重启，**以为修好了而实际毫无变化**。同一文件里 `auth.enabled` 真读、`server.*` 不读，没有任何标记区分。修法：要么真消费它，要么从类型与示例配置里删掉 | open |
| F-016 | 2026-07-27-08 | **high** | `shared/dto/passport.dto.ts` `PassportLinkStartRequestSchema` | `.regex(/^https?:\/\//)` **明确允许 `http://` 到任意主机**（为本机 dev 站开的口子放得太宽）。用户填非本机 http 地址后：每次轮换明文 POST `refreshToken`；`unlink` 明文 POST `refreshToken`；**备份上传把整个 State Root（workspace + `config.yaml` + 含 `NUXT_SESSION_PASSWORD` 的 `.env`）连同 Bearer 明文发出**。修法：`http://` 只放行 `localhost`/`127.0.0.1`/`[::1]`。触发需用户主动填写，仓库无内置 http 默认值 | open |
| F-013 | 2026-07-27-07 | medium | `server/backup/backup-job-manager.ts` `runBackup`（约 125–140）+ `server/passport/passport-client-service.ts` `unlink` | `runBackup` 在第 125 行把 token 拷进局部 `fresh`，随后上传整个 State Root（含 config.yaml / .env）。`unlink()` 只清 `accessTokenCache`，**清不掉已拷出的 bearer token**，上传照常跑完 → 用户点了「取消关联」，数据仍在往那个账号传。且 `unlink` 对官方站 revoke 的失败被 `catch {}` 吞掉。不需要竞态，只要上传在途。修法：加凭据代次标记，`unlink` 递增，上传分片前校验 | open |
| F-014 | 2026-07-27-07 | low | `server/passport/passport-client-service.ts` `refreshAccessToken` / `unlink` | `refreshAccessToken` 顺序是 `findUnique` → `$fetch` → `saveCredential`(upsert)。`unlink` 的 `delete` 若落在 `$fetch` 与 upsert 之间，**凭据行被重新创建、账号悄悄恢复关联**。窗口 = 一次网络往返，故 low；但若 revoke 也失败（被吞），复活的是完全可用的凭据。同一个代次标记可一并修掉 | open |
| F-015 | 2026-07-27-07 | low | `server/passport/passport-client-service.ts` `startLink` / `linkSessions` | `linkSessions` Map 只在 `pollLink` 的四条出口删除，**发起关联后不轮询（关掉对话框）就永久泄漏一条含 `deviceCode` 的会话**，无 TTL 清扫、只增不减。单实例单用户增长率低故 low，但存的是早已过期的凭据类秘密。修法：`startLink` 时顺手清掉 `expiresAt < Date.now()` 的条目 | open |
| F-012 | 2026-07-27-06 | low | `server/agent/skills/llmlint.test.ts`（配置字面量，约 169–964 的十余处） | **实测确认** `bun run typecheck` 恒定 26 错：25 项 TS2345 都是「`NormalizedLlmlintConfig` 新增必填 `ignoreTerms`，测试里的配置字面量没跟上」，1 项 TS2322 是 `'"module"' not assignable to '"builtin"'`。后果是 typecheck 永远不干净，**当不了 CI 门禁，新错误也埋在噪音里**。修法很小：补 `ignoreTerms` 或抽 `makeConfig()` 帮手。Task 123 已记录过错误条数，本条补充根因与「工具被钝化」的代价 | open |
| F-010 | 2026-07-27-05 | medium | `server/backup/backup-archive-rules.ts` `shouldExcludeFromBackup` | 排除判据是 `name.endsWith(".lock"\|".tmp"\|"-wal"\|"-shm")`，**按文件名后缀匹配、无目录作用域**，对用户 manuscript 目录同样生效。一个叫 `草稿.tmp` 的稿子会静默且永久地不进任何备份。与 F-009 不同的是**这里根本不产生错误**，再好的错误处理也发现不了。修法：wal/shm 收敛到「同目录有同名 `.sqlite`」或限定 `.nbook/` 内 | open |
| F-011 | 2026-07-27-05 | low | `server/backup/backup-archive-rules.ts` `isSqliteFile` | 只认 `.sqlite`，而 `server/workspace-files/workspace-files.ts`（约 151/179/180）同时登记 `.db` / `.sqlite` / `.sqlite3`。已 grep 确认今天全仓 DB 都叫 `*.sqlite` 故未命中；一旦将来有 DB 用别的扩展名，会被原样拷贝而不走 VACUUM 冷快照，同时它的 `-wal` 仍被排除 → 备份里是缺了未 checkpoint 写入的残库。埋雷，与 F-004 同类 | open |
| F-009 | 2026-07-27-04 | **high** | `server/backup/backup-archive-service.ts` `collectFiles`（约 47–53）与顶层文件收集（约 96–102） | 裸 `catch { return; }` 吞掉 `readdir` 的全部错误，注释却写「目录不存在」。任一子目录遇 `EACCES`/`EPERM`/`EIO`，**整棵子树静默消失在备份里**，备份仍返回成功 + sha256 + 空 warnings，用户到恢复时才发现缺了正文。同文件 `snapshotSqlite` 调用点（约 165–173）证明 warnings 上报通道存在且可用，`collectFiles` 签名里根本没接。修法：只把 `ENOENT` 当正常缺失，其余接进 warnings | open |
| F-008 | 2026-07-27-03 | **high** | `server/config/config-service.ts` `readJsonFile` / `readJsonFileSync` / `writeJsonFile` | 与 F-006 逐字同构的模式，但对象是 Global / Project Config（`workspace/.nbook/config.json`）：写入裸 `fs.writeFile` 非原子，读取只兜 `ENOENT`、`JSON.parse` 的 SyntaxError 直接抛。config 每改一次设置就写一次，崩溃窗口命中概率远高于 sync state。**修法必须与 F-006 不同**——config 是真实用户数据（含 API key），不能「解析失败就当不存在」，只能写入原子化 + 指名道姓的错误信息。已用 `git show HEAD:` 对已提交版本确认（该文件当前是热区） | open |
| F-007 | 2026-07-27-03 | medium | `server/workspace-files/novel-workspace.ts:1410` | `replaceFileWithRollback` 名字像原子替换，实为**进程内事务回滚**：第 1420 行仍是就地 `fs.copyFile`，进程被杀时 catch 不执行、目标照样半截。**因此 profile 与 variable 两条链的崩溃窗口与 asset 链完全相同，「改用 replaceFileWithRollback」不是 F-005 的修法**。附带：进程死在 commit 前会残留 `<目标>.{uuid}.backup`，无任何启动清理路径 | open |
| F-005 | 2026-07-27-02 | medium | `server/workspace-files/novel-workspace.ts:555` | `syncManagedSystemAssetsToUserAssets` 三处写入（555 / 577 / 609）全是 `fs.copyFile` 就地截断重写，非原子。被打断留下的半截文件**永不自愈**：非 force 路径下必落到 587–593 或 594–601 两条 warning 分支之一，把截断文件当成「用户手改」保护起来，用户看到的提示与真相相反。是 F-001 的后果承载体。**第 3 轮修正：影响面是全部三条同步链**（profile / variable 链用 `replaceFileWithRollback`，但那不是崩溃原子性，见 F-007） | open |
