起: 1785103477 (2026-07-27 06:04:37)
止: 2026-07-27 06:16 左右

# 第 11 轮 · 收敛：复核 19 条 finding，并把早报提前写完

## 运行身份判定

- `running`，距上次唤醒 1200 秒 = 20.0 分钟 < 断线阈值 50 分钟 → **同一次运行**
- 剩余 55 分钟（约 2.7 轮）
- `rounds/` 无孤儿文件
- 基线 135f，**连续第六轮逐字持平**

## 选向理由

按第 9 轮定的收敛安排，本轮不查新代码。但**把「写早报」从 Phase 7 提前到本轮**，这是对原计划的一处偏离，理由：

会话整夜被 compact 多次，Phase 7 那一轮如果只剩下「凭记忆总结 19 条」，质量完全取决于那一刻上下文里还剩什么。**现在写，`FINDINGS.md` 和十份 round 文件都还在手边。** Phase 7 就只剩改两个数字和删 job——那两件事不依赖上下文。

## 做了什么

### 1. 复核 finding 位置是否仍然可信

`git status --short` 逐个查 19 条 finding 涉及的全部文件：

```
server/backup/  server/passport/  server/config/  server/runtime/
shared/dto/passport.dto.ts  server/workspace-files/novel-workspace.ts
scripts/deploy/product-start.mjs  packages/neuro-book-manager/src/
```

**只有 `server/config/config-service.ts` 被改过**（F-008 所在）。其余全部未被并发会话触碰，位置与机制描述仍然成立。

### 2. 复核 F-008 是否已被并发会话修掉

`git diff --stat` 显示 7 insertions / 4 deletions。对 diff 内容 grep `readJsonFile` / `readJsonFileSync` / `writeJsonFile` / `JSON.parse` / `fs.writeFile`：**零命中**。三个函数在工作副本里仍在（行号从 1547/1558/1569 漂到 1550/1561/1572，因为上方有无关改动）。

**F-008 仍然成立。**

> 这里印证了一条纪律的价值：`FINDINGS.md` 的条目**写的是函数名不是行号**，所以文件被改动 3 行之后条目仍然可用，不需要重新定位。

### 3. 复核有没有第二个「机制写错」的 finding

第 9 轮的教训是 F-017 的机制写反了。本轮逐条把 19 条的修法在脑子里跑一遍「按这个改完，问题真的会消失吗」：

- F-006 的链条最长（非原子写 → 解析失败抛 → 冒泡穿过 `prepareSystemAssets` → `product-start.mjs` 顶层 await 无 try/catch → 起不来），**四个环节都在第 2、3 轮实读过**，成立
- F-007 本身就是「修法会失效」的那条警告，它保护的是 F-005，成立
- F-013 的修法（凭据代次标记）要求消费点在每个网络分片前校验——**修法本身没问题，但它需要改 `backup-job-manager` 而不只是改 `passport-client-service`**，已在条目里写明
- 其余各条的修法都落在被观察到的那一行上

**没有找到第二个 F-017。**

### 4. 写 `REPORT-2026-07-27.md`

按 REFERENCE 的格式，加了几处偏离：

- **开头加了一整段免责**：今夜 19 条**全部**没经过编译或测试验证。这是本次运行最重要的元信息，不能埋在末尾
- **「今天值得动手的」按 4 组而非 4 条**：F-017+F-018 合并（后者是前者的放大器）、F-006+F-008 合并（同模式但**修法必须不同**，这点单独加粗）
- **加了「两条主线」一节**：19 条逐条看没用，两条病因（catch 的宽窄不一致 ／ 为本地开发开的口子没限定在本地）才是能一次修掉的东西
- **加了「本仓库里写对了的地方」一节**：六处正面样板，修的时候照抄。今夜几次发现「同一文件里既有写对的也有写错的」，给出样板比给出建议更省事
- **F-017 特意保留了第 8 轮写错、第 9 轮自我推翻的经过**：不写出来，看报告的人可能正好按错误机制去改

## 与原计划的偏离

原第 11 轮任务是「复核 + 整理两条线结构 + 补覆盖率地图」。实际把「写早报」也做了（原属第 12/Phase 7）。覆盖率地图在第 10 轮末已回写完毕，本轮无需再补。

## 剩余安排

- **第 12 轮（06:24）**：若基线仍静止，可考虑跑一次 `bunx vitest run server/backup --testTimeout 20000` 之类的定向单测——**但只在能证实某条 finding 时才跑**，否则复核 `CHECKLIST.md`（今夜加了 7 组，只增不删，可能已有重复）
- **06:44 那轮**：缓冲
- **07:04（过截止）**：Phase 7 —— 只需改 REPORT 的两个数字、RUN 块落 `finished`、`CronDelete`
