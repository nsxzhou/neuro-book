起: 1785105876 (2026-07-27 06:44:36)
止: 2026-07-27 06:52 左右

# 第 13 轮 · 补掉唯一的欠项：复核 CHECKLIST

## 运行身份判定

- `running`，距上次唤醒 1201 秒 ≈ 20.0 分钟 < 断线阈值 50 分钟 → **同一次运行**
- 剩余 **15.4 分钟**，单轮预算 15 分钟 —— 15.4 > 15，按 Phase 1 判据**继续跑一轮**（差 24 秒，但规则是规则）
- `rounds/` 无孤儿文件
- 基线 130f，**新口径生效后第一次两侧同口径比对，逐字持平** → 第 12 轮的修法确实起作用了

## 选向理由

第 12 轮记了唯一一件「计划了却没做」的事：复核 `CHECKLIST.md`。本轮把它补上，这样 Phase 7 交接时没有欠项。

不开新方向——剩余时间只够做这一件，而且第 10 轮已经宣布探索结束。

## 做了什么

通读 `CHECKLIST.md`（135 行、11 组，一夜从 25 条涨到 60+ 条），找重复与矛盾。**规则是只增不删、删除须人工**，所以只列问题、不动手，结果写成文件末尾的「待人工整理」一节。

### 找到一处真矛盾

- 第 4 轮 L60：裸 `catch {}` 后跟 `return`/`continue`「**是静默丢数据的标准形状**」
- 第 5 轮 L74：「grep 出来的是候选池，不是发现……**裸 catch 本身不是信号**」

**这两条会导出完全相反的行为。** 第 5 轮是实测出来的（全仓 106 处，抽最高危的 `workspace-history` 7 处逐个读，零缺陷），第 4 轮是当时的印象。留着第 4 轮那条，下一晚会照它把一百多处裸 catch 当发现报出来——**正好是第 5 轮花一整轮证伪掉的做法**。

建议保留第 5 轮，把第 4 轮那条降级成「候选池的入口」。

### 三处逐字重复

第 4 轮 L61 ↔ 第 5 轮 L72（注释与 catch 宽度不符）；第 4 轮 L62 ↔ 第 5 轮 L71+L73（上报通道没接）；第 8 轮 L109 ↔ 「high 的代价」组（多前提要逐条标注）。

三处都是**后写的那条更精确**，因为多经历了一轮实测。

### 一处阈值不一致

「同类点挖到第几个该停」这一件事，今夜给出三个不同的数：第 3 轮说**第三个实例**、第 6 轮说**第 6/7/8 个实例边际价值极低**、第 6 轮又说**连查三轮换方向**。

三条都对，但一条按实例数、一条按轮数，混在一起没法执行。已给出合并建议。

### 一条结构建议

「通用」「本仓库特化」是**入口清单**（每轮扫），后九组是**方向专用**（相关时才调）。这个分工没写下来，导致 Phase 1 那句「条目超过 20 条时只挑与当前方向相关的」没有可操作依据。

## 本轮的元观察

**这份 checklist 一夜涨了一倍多，而且已经开始自我矛盾。** 「只增不删」的规则保护了它不被误删，代价是它会持续劣化到需要人工介入。

这不是设计缺陷——夜审无人值守时**没有能力判断哪条该删**（第 4 轮那条听起来完全合理，只有做过第 5 轮的实测才知道它错）。正确的分工就是：**夜审只管加和标注冲突，删除留给人**。本轮做的正是这件事的第一次实践。

## 未完成的部分

无。今夜所有计划项已清空。

## 交接给 Phase 7

- `REPORT-2026-07-27.md` 第 11 轮已写好，只需改顶部的「完成轮数」与「实际停止」，并删掉草稿状态那行
- RUN 块落 `状态: finished` + 实际停止时间，**先落盘再删 job**
- `CronList` → 匹配 prompt 含 `night-audit` 且 tracking-dir basename 完全等于 `127-nightly-audit`，**恰好一个才删**（job id `2609cd53`）
- `PushNotification` 一条（运行窗口结束，属允许通知的三种情况之一）
