---
schema: nbook.walkthrough/v1
taskId: 00149-monorepo-workspace-consolidation
sequence: 001
role: leader
status: in-progress
createdAt: 2026-08-16T14:59:07Z
---

# Leader：批准后启动

## 范围

用户已批准 `local://monorepo-main-app-migration-plan.md`。本次实现严格按 S0–S8 执行，主应用、六个自治项目、contracts/test-support、发布/Docker/CI 和治理文档均在范围内；真实发布、远端写入和原仓修改不在范围内。

## 初始证据

执行前已读取权威计划、根 `AGENTS.md`、Task 规则、贡献规则、测试规则、包规则、脚本规则和发布规则。当前 root `master` 的 `HEAD` 为 `306e563ad7a4d4a58354fa8d582ad9aa9b886e8c`；工作树同时存在上一轮治理迁移的暂存改动和用户未暂存改动，后续基线只收纳明确归属的治理迁移，原用户改动保持原地。

## 下一步

生成 S0 内容级证据，执行基线门禁；仅在验证结果可追溯后创建 baseline commit/tag 与迁移 worktree，并把 worktree/branch 身份回写 Task。
