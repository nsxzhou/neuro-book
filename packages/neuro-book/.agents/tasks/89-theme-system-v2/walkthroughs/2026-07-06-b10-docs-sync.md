# B10 文档同步收尾

## 本轮目标

按 PLAN.md B10 与 README Goal 的硬性要求，同步主题系统规范文档和项目状态文档，确保后续 Agent 能按 v2.1 主题变量体系与自定义主题契约继续开发。

## 实际变更

- 更新 `AGENTS.md`
  - 通用组件索引补 `FormColorField.vue`。
  - HTML/Vue 规范新增主题变量硬约束：禁止新增 Tailwind 调色板类与 `dark:`，状态语义映射口诀，分类色板例外，新增组件层变量登记规则。
- 更新 `CLAUDE.md`
  - 同步 HTML/Vue 主题变量硬约束，保持与 AGENTS.md 规则一致。
- 更新 `app/utils/theme/README.md`
  - 补充 shared 契约源、runtime / Global Config 存储、bootstrap 时序、`resolveTheme()`、自定义主题缺键补齐规则。
  - 补充自定义主题编辑器、JSON 导入导出、`color-mix(...)` 解析、核心 13 色与高级 36 变量编辑规则。
- 重写 `reference/theme/system.md`
  - 移除旧的“三主题 + localStorage”说明。
  - 改为 v2.1 稳定参考：事实源、运行时流程、存储契约、变量体系、自定义主题、消费规则、状态语义、分类色板例外和验证建议。
- 更新 `PROJECT-STATUS.md`
  - Recent Tasks 新增 Task 89 状态，记录 36 变量体系、自定义主题、文档同步、已知验证基线和待浏览器验收。
- 更新 `docs/tasks/89-theme-system-v2/README.md`
  - Current State 改为 B1-B10 已完成。
  - Relative docs refs 改为稳定规范文档。
  - Verification 记录最终已知状态。
  - Implementation Walkthrough 链接 B1-B9 报告。
  - TODO 补浏览器验证待授权清单。

## 与计划的出入

- B10 要求的五份规范/状态文档均已同步，额外更新了任务 README 自身的执行状态与 walkthrough 索引。
- 未执行浏览器验证，原因是用户明确要求浏览器验证前必须先征求同意；本轮只记录待授权验收清单。
- 未修改非主题基线失败，也未用 hack 绕过类型系统。

## 验证结果

- `rg -n "localStorage|3 主题|三主题|内容已过时|尚未开始编码|prompt-bg|agent-bg" ...`：无命中，旧主题契约已清理。
- `bun run typecheck`：失败。
  - 失败仍集中在既有非主题基线：Agent plan-mode / `agentMode` 类型漂移、Profile DSL `PlanMode*` 导出缺失、writer fixture `customTopSystemPrompt`、`server/low-code-form/index.ts(798,13)`。
- `bun run test`：失败。
  - 最终统计：17 failed / 154 passed / 1 skipped test files；45 failed / 1254 passed / 87 skipped tests；2 unhandled errors。
  - 失败集中在既有 Agent/Profile/Workspace/Auth/Web/Plot 相关超时或 plan-mode 契约问题。
- `bun run generate:openapi`：通过。
  - 40 routes updated，0 failed。

## 遗留与风险

- 主题系统代码、配置和文档已完成 B1-B10；浏览器端自定义主题全流程仍需用户授权后验证。
- 推荐浏览器验证清单：新建→调色→覆盖→重新生成→保存→切换→导出→删除→导入→重启保持→Agent 链路切主题。
- 全量 typecheck/test 仍受既有非主题基线阻塞；本任务未处理这些历史问题。
