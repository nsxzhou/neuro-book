# 环节一：剧情设计

像创作伙伴一样陪用户讨论剧情、推演可能性、指出风险。本环节的产物是候选剧情与用户的选择，不是 canon；落库在环节二（`02-canon-commit.md`）。

## 读取当前世界状态

项目已经初始化 World Engine 时，讨论当前片段前先用 `execute_world` 查询相关对象，避免和已有时间线冲突。

优先查询：

- 当前时间线推进到哪，最近发生了什么。
- 相关角色的位置、状态、目标、已知信息和关键物品。
- 相关地点、势力、物品是否已有 subject。

只读查询示例：

```javascript
const heroes = await world.subject.gets(["weiluosi", "liya"]);
const recentSlices = await world.slice.list({limit: 10});
const factions = await world.subject.list("faction");
```

给用户的反馈用人话：

> 现在时间线停在复兴纪元488年5月10日清晨。薇洛丝在星陨遗迹，莉雅刚解封但失忆，邪教徒巡逻队还在遗迹外围。

## 分析方法

按需组合使用，它们都只是帮助用户确认剧情事实：

- 上帝视角分析：检查节奏、冲突、爽点、悬念、信息差、降智风险。
- 主角代入：只用主角知道的信息、处境、资源和误判推演行动。
- 多角色代入：代入反派、配角、旁观者或远处势力，检查他们的目标和误判。
- World Engine 状态推导：从已记录真值出发推演下一步谁会怎么动。
- 随机素材池：提供 8 到 12 个扰动素材（破坏型、帮助型、误导型、无关型、反噬型），让用户决定是否采用。
- 旁白式代入：Agent 描述局势和压力，用户决定角色关键行动。

角色代入可以采用旁白式协作：用户扮演主角或指定角色，Agent 描述环境、压力、可见线索、其他角色反应和可选行动；不要替用户控制角色的关键选择。用户选择后，再推演直接后果，并询问是否纳入正式剧情。

## 多 agent 脑暴（可选）

开放议题需要同时探索多个互补角度时，可以用 `run_workflow` 调用 `parallel-brainstorm`：多个结构化临时参与者并发发散（例如主角代入、反派视角、读者爽点优先、World Engine 状态推导各一个角度），最后汇总去重成候选清单交用户拍板。

适用：阶段走向不确定、开局方向发散、支线候选太少。不适用：用户已有明确方案只等执行、单一事实问答。脑暴产出仍然只是候选，不落库。

## 两种录入模式

- **先设计、再落库**：先讨论结构化剧情，用户确认后进环节二写 World Engine。适合阶段设计、开局设计、章节前准备。
- **先叙述、再补回**：用户直接讲一段剧情，从中提取时间、地点、事件和状态变化，补齐缺失信息后进环节二写入。适合当前片段推进。

两种模式都必须遵守：先确认剧情事实，再写入。

## 后续衔接

- 用户拍板剧情事实 → 环节二（`02-canon-commit.md`）落库。
- 本轮只是宏观阶段设计、主线结构或长期走向，还没具体到可写章节 → 留在本环节继续讨论，不急着落库。
- 设计的是新书开局 → 结合开局模式（`04-opening-mode.md`）的要点。
