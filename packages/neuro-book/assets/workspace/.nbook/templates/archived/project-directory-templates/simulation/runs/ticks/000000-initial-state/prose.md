# Initial State Prose

本文件保存用户可见正文。初始化 tick 通常可以留空；需要开场说明时，由 `rp.writer` 根据 `rp.leader` 提供的 Writer Brief 写入一段不泄露隐藏真相的开场正文。

正式 RP Tick 中，`rp.writer` 输出给用户的完整正文应保存到对应 tick 的 `prose.md`。`rp.leader` 只组装正文链接和元场景，不直接写正文。正式章节正文仍以 `manuscript/.../index.md` 为主。
