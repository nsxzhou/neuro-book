# World Engine 写作模式工作流

> 本文讲清"写作模式"下 World Engine（世界引擎）的整体工作流与各系统职责边界，是把所有概念串起来的宏观总览。读者是 leader 与 writer。
>
> 概念契约细节见同目录其它 reference；具体操作步骤是 skill 的职责，本文只讲原理、契约与"为什么"。

## 1. World Engine 在写作模式中的定位

World Engine 是项目内**动态世界状态 + 时间线的唯一真相源**。

它用事件溯源（event sourcing）表达世界演化：世界不存"当前状态"，只存按时间排列的 **slice（切面）** 序列。任意时刻的世界状态 = 该时刻之前所有 slice 按时间 reduce（归约）出来的结果。每个 slice 记录某个时间点上一组 **patch（变更）**，每条 patch 描述对某个 **subject（主体）** 的某个 JSON Pointer **path（路径）** 做某种 **op（操作）**。

关键术语在第一次使用时点明：

- **slice / 切面**：一个时间点 + 该时间点发生的一组变更。同一个时间点只能有一个 slice。
- **subject / 主体**：能独立演变状态的对象。不限于人物，地点、势力、王国、任务、重要物品都可以是 subject。
- **instant**：底层时间真相源，一个连续单调的整数刻。用户侧用项目日历字符串表达；`execute_world` 沙箱内用 `world.time.parse()` / `world.time.format()` 在日历字符串与 instant bigint 之间转换。
- **patch / 变更**：对某 subject 某个 path 的一次操作。
- **op / 操作**：patch 的动作类型，全集为 `replace` / `increment` / `remove` / `append`。
- **reduce / 归约**：把一段 slice 序列叠加算出某时刻状态的过程。

因为状态由 slice 序列 reduce 得来，**往时间线更早处插入一个 slice 就能补历史、补设定**：补一段人物过去、补一块大陆历史，都只是"在合适的时间点新增一个 slice"，后续状态会自然叠加上这条新变更。这是 World Engine 区别于"直接存一份当前状态"的根本能力。

## 2. 关注度等级系统

在世界引擎里记录角色时，需要按**关注度等级**决定为每个角色建立多少条 backstory 切片、记录多细的状态变化。关注度等级用星级标注，从 ★☆☆☆☆（临时 NPC）到 ★★★★★（绝对主角）。

### 2.1 五级定义

| 星级 | 等级名 | 切片密度 | 说明 | 示例角色 |
|------|--------|----------|------|----------|
| ★★★★★ | 绝对主角 | 极细 | 完整生命线，每个关键转折都有切片；当前剧情每个场景转换或状态变化都记录 | 第一人称主角、双主角之一 |
| ★★★★☆ | 核心主角 | 细 | 主要生命线节点 + 当前剧情重要场景；backstory 5-8 条 | 主要视角角色、核心队友 |
| ★★★☆☆ | 重要配角 | 中 | 关键背景事件 + 当前剧情里的重要行动；backstory 2-4 条 | 重要反派、关键 NPC、阶段性队友 |
| ★★☆☆☆ | 次要配角 | 粗 | 当前处境 + 1-2 条关键背景；只在直接参与剧情时记录状态 | 城主、商会长、短期任务委托人 |
| ★☆☆☆☆ | 临时 NPC | 不建 subject | 只在主角 events 文本里提及，或用群体 subject 表示；不单独追踪状态 | 路人、店员、一次性敌人 |

### 2.2 Lorebook 标题格式

在 lorebook 角色目录的 `index.md` 标题里标注星级，格式为：

```markdown
# 艾莉娜·晨曦（Erina）[★★★★☆ 主角]
```

这个标注帮助 leader 快速判断为该角色建立多少条 backstory 切片。

### 2.3 关注度与 backstory 切片数量

| 星级 | backstory 切片数量 | 典型内容 |
|------|-------------------|----------|
| ★★★★★ | 5-10 条 | 出生、童年转折、习得关键能力、重大创伤、加入组织、关键决策、近期处境 |
| ★★★★☆ | 5-8 条 | 出生/来历、关键转折、习得能力、加入当前组织、近期处境 |
| ★★★☆☆ | 2-4 条 | 来历、关键转折、当前处境 |
| ★★☆☆☆ | 1-2 条 | 来历或当前处境（二选一，或只记录当前处境） |
| ★☆☆☆☆ | 0 条 | 不建 subject |

### 2.4 动态调整

角色的关注度等级可以随剧情阶段调整。例如：

- 第一卷的核心反派（★★★☆☆）在第二卷被击败后降为次要配角（★★☆☆☆），不再追踪详细状态。
- 第一卷的次要配角（★★☆☆☆）在第二卷成为队友后升为核心主角（★★★★☆），补充更多 backstory 切片。

调整时不需要删除旧切片，只需停止或开始记录新状态变化即可。

## 3. LOD 边界指南

LOD（Level of Detail）是写作模式中控制记录粒度的判断工具：它决定哪些内容要成为 World Engine subject，哪些只作为场景氛围、群体动向或 writer brief 的背景素材。核心边界是：**LOD 可以描述环境、氛围、群体动向；一旦个体有名字、会对话、会再次出现或需要追踪状态，就升级为 subject**。

### 3.1 写作模式 LOD 表

LOD 用于给 writer 提供"角色能感知到的场景细节"，并帮助 leader 判断哪些事实需要进 World Engine：

| LOD | 适用范围 | World Engine 记录原则 |
| --- | --- | --- |
| **LOD0 当前场景细记** | 当前视角附近，角色能直接感知并互动的角色、地点、物品、伤势、线索 | 细记。命名角色、重要物品、位置、伤势、获得信息等通常要写入相关 subject。 |
| **LOD1 区域中粒度** | 同一地点或附近区域，能影响当前场景的其他角色、势力或事件 | 中等粒度。只记录会影响当前剧情的动向；群体可先用整体 subject 或事件摘要。 |
| **LOD2 远处粗记** | 远方势力、背景变化、伏笔和世界事件 | 粗记。通常一条摘要 slice，不展开细节个体。 |
| **LOD3 氛围/群体默认不建 subject** | 天气、路人、城镇氛围、普通巡逻、背景人群 | 一般不建 subject，只写进相关事件摘要或 writer brief。 |

### 3.2 关键约束：不引入有名字的个体

❌ **错误示例**：
- "女仆莉丝端着茶盘走进客房"
- "卫兵队长格里芬正在训练新兵"
- "商人艾德温在大厅等候觐见"

✅ **正确示例**：
- "一名女仆端着茶盘走进客房"
- "卫兵队长正在训练新兵"
- "一位商人在大厅等候觐见"

### 3.3 何时从 LOD 提升为 subject

当某个 LOD 里的个体需要**与主角直接对话、做出独立决策、追踪状态变化**时，立刻为其建立 subject。例如：

- 场景 1（LOD）："一名女仆端着茶盘走进客房"
- 场景 2（主角与她对话）：立刻建立 subject "莉丝"（★★☆☆☆ 次要配角），补 1 条 backstory 切片记录她的来历和当前职责
- 场景 3 之后：在切片里记录她的对话内容和状态变化

不要提前为"可能会用到的角色"建 subject。等到真正需要时再建，保持"最少支持当前叙事"原则。

## 4. 切片粒度控制

切片粒度（一个切片覆盖多长的戏内时间、记录多少细节）需要动态调整，而不是固定标准。

### 4.1 动态调整规则

| 维度 | 细粒度（更多切片） | 粗粒度（更少切片） |
|------|-------------------|-------------------|
| **空间** | 主角当前场景，视角附近 | 视角之外的远处事件 |
| **时间** | 新发生的事件 | 旧事件（backstory） |
| **重要性** | 关键转折、战斗、对话 | 赶路、日常、过渡 |
| **信息密度** | 多个角色同时行动、状态频繁变化 | 单一角色的单调行为 |

### 4.2 场景类型对照表

| 场景类型 | 推荐粒度 | 示例 |
|---------|---------|------|
| **战斗** | 极细（每个回合或关键动作一条切片） | 主角挥剑 → 敌人格挡 → 主角受伤 → 队友支援（4 条切片） |
| **重要对话** | 细（每个话题转换或情绪转折一条切片） | 自我介绍 → 坦诚困境 → 建立信任（3 条切片） |
| **探索/移动** | 中（每个地点转换或发现一条切片） | 离开祭坛 → 前往客房 → 到达客房（3 条切片） |
| **日常/休息** | 粗（整个时间段一条切片） | 用餐、睡觉、赶路（1 条切片） |
| **backstory** | 粗（一个人生阶段或关键事件一条切片） | "478-487年：继承爵位，领地衰败"（1 条切片） |

### 4.3 实战案例：第一章剧情

《命定之诗2》第一章覆盖 5 小时戏内时间（488年5月10日 00:05 ~ 05:00），使用 4 个切片：

1. **00:05**（10 分钟）：初次对话，身份与来历
2. **00:30**（30 分钟）：前往客房，初步探索
3. **03:00**（1 小时）：深夜对话，建立初步信任
4. **05:00**（瞬间）：黎明决定，临时共识

平均每个切片覆盖 1-2 小时戏内时间。如果是战斗场景，同样 5 小时可能需要 10-15 个切片；如果是赶路场景，可能只需 1-2 个切片。

## 5. 初始化流程

World Engine 初始化的完整流程分为 5 个 Phase，每个 Phase 有明确的输入、输出和验收标准。

### Phase 0：用户准备（leader 引导）

**目标**：收集足够的初始化信息。

**需要确认**：
- ✅ 剧情大纲（至少第一卷/第一章的框架）
- ✅ 主要角色列表（至少 2-3 个核心角色）
- ✅ 世界观要点（纪年、地理、力量体系概要）
- ✅ 故事"现在"是哪个时间点

**不需要**：
- ❌ 完整世界观百科
- ❌ 所有配角的详细设定
- ❌ 完整章节大纲

**输出**：用户确认可以开始初始化。

### Phase 1：Era 切片（world subject）

**目标**：建立纪元锚点，定义零点。

**操作**：
1. 根据用户提供的纪年系统，创建 world subject（如"阿斯塔利亚"）
2. 写入第一个切片（通常是纪年第 1 年第 1 月第 1 日）
3. 在 summary 里说明"本纪元起始于某某事件"

**验收**：
- `world.subject.list("world")` 返回 1 个 world subject
- `world.time.now()` 能返回正确的当前时间（故事"现在"）

### Phase 2：初始化核心 subject

**目标**：按第一卷框架建立核心角色、关键势力、主要地点。

**操作**：
1. 为每个核心角色（★★★★★ 或 ★★★★☆）建立 subject
2. 为主要势力（帝国、组织、部落）建立 faction subject
3. 为主要地点（王都、主角家乡、冒险起点）建立 location subject

**验收**：
- `world.subject.list("character")` 返回核心角色列表
- `world.subject.list("faction")` 返回主要势力列表
- `world.subject.list("location")` 返回主要地点列表
- 每个 subject 都有 `name` 和基础字段（如角色的 `age`、`sex`）

### Phase 3：Backstory 溯源

**目标**：按关注度等级为每个角色补充 backstory 切片。

**操作**：
1. ★★★★★ 角色：补 5-10 条 backstory 切片（完整生命线）
2. ★★★★☆ 角色：补 5-8 条 backstory 切片（主要节点）
3. ★★★☆☆ 角色：补 2-4 条 backstory 切片（关键背景）
4. ★★☆☆☆ 角色：补 1-2 条 backstory 切片（当前处境）

**时间选择**：
- 用精确时间（如"453年3月15日：维克托出生"）
- 或模糊时间段（summary 文本表达，如"453-470年：在帝都接受贵族教育"）

**验收**：
- `world.subject.get(角色id).events.length` 符合预期数量
- 每个角色的 backstory 切片时间早于故事"现在"

### Phase 4：确认故事起点

**目标**：写入"故事开始那一刻"的状态，确认所有角色的初始位置、状态、目标。

**操作**：
1. 写入故事"现在"时间点的切片（如"488年5月10日 00:00：召唤仪式成功"）
2. 确认每个角色的 `location`、`hp`、`goals`、`inventory` 等初始状态
3. 向用户展示"当前世界状态摘要"

**验收**：
- 所有核心角色都有明确的当前位置
- 故事起点的切片是时间线上最新的一条
- `world.slice.list()` 返回的切片列表符合预期

**输出**：World Engine 初始化完成，可以开始剧情推进或正文写作。

## 6. 剧情推进流程

剧情推进是 leader 的核心工作，分为 4 个 Phase，每轮推进都走一遍这个循环。

### Phase 1：剧情设计（Leader + 用户协作）

**目标**：确定下一段剧情的框架。

**Leader 的职责**：
- 读取当前世界状态（用 `execute_world` 查询角色位置、状态、目标）
- 向用户提出 2-4 个候选方向（基于当前状态和用户之前表达的意图）
- 确认本轮剧情的目标、范围、关键事件、预期结局

**用户的职责**：
- 选择方向或提出自己的想法
- 确认关键情节（哪些角色参与、发生什么、如何结束）
- 明确信息控制要求（谁知道什么、谁不知道什么）

**输出**：剧情框架（可以是自然语言描述，也可以是结构化清单）。

### Phase 2：写入切片（Leader 操作）

**目标**：把剧情框架转化为 World Engine 切片序列。

**操作步骤**：
1. 把剧情框架拆分为若干场景或关键状态变化
2. 为每个场景确定时间点（精确到小时或分钟）
3. 为每个场景写入切片（`execute_world` 中调用 `world.slice.write`）：
   - 更新角色 `location`（如果移动）
   - 更新角色 `hp` / `mp`（如果战斗）
   - append 到角色 `events`（记录发生了什么）
   - 更新角色 `relationships`（如果关系变化）
   - 更新角色 `knowledge`（如果获得新信息）

**粒度控制**：参考第 4 节的场景类型对照表。

**验收**：
- 每个切片的时间点符合剧情顺序
- 所有角色的状态变化都已记录
- `execute_world` 查询结果符合预期

### Phase 3：写正文（Writer 基于世界状态）

**目标**：让 writer 基于已推进的世界状态写出章节正文。

**Leader 的职责**：
1. 准备简化 brief（见 workflow.md 第 6 节）
2. 调用 writer（`invoke_agent`）
3. 传递 brief + lorebook 路径 + World Engine 查询提示

**Writer 的职责**：
1. 读取 brief 和 lorebook
2. 用 readonly `execute_world` 查询角色当前状态
3. 写出符合状态的章节正文
4. 报告完成情况

**关键原则**：
- **世界状态先行**：leader 在调用 writer 之前已经把状态推进好
- **brief 简化**：只传框架，不传可查询的细节（见 workflow.md 第 6.2 节）
- **信息控制**：明确每个角色知道什么、不知道什么

**输出**：章节正文写入 `manuscript/.../index.md`。

### Phase 4：回顾与调整

**目标**：检查成果，处理偏离和意外。

**Leader 的职责**：
1. 读取 writer 写出的正文
2. 检查是否有"偏离 brief 的状态变化"（如 writer 自由发挥增加了新角色、改变了受伤程度）
3. 如果有偏离：
   - 向用户确认是否接受
   - 如果接受，补写切片（创建新角色 subject、更新状态）
   - 如果不接受，让 writer 重写或回退切片
4. 向用户展示"本轮剧情摘要 + 当前世界状态"

**用户的职责**：
- 确认是否满意
- 提出修改要求或继续下一轮

**输出**：本轮剧情推进完成，世界状态与正文同步。

## 7. Lorebook 与 World Engine 职责边界

Lorebook 与 World Engine 的职责边界最容易混淆，这里用表格点清楚。

### 7.1 静态 vs 动态对照表

| 信息类型 | Lorebook | World Engine | 示例 |
|---------|----------|--------------|------|
| **年龄** | 初始年龄或外貌年龄 | 当前年龄（会随时间变化） | Lorebook: "外貌19岁"；World Engine: `age: 19`（初始）→ `age: 20`（一年后） |
| **关系** | 稳定关系标签 | 当前关系状态与历史变化 | Lorebook: "薇洛丝的队友"；World Engine: `relationships: [{target: "veiluosi", type: "ally", since: "488年5月10日"}]` |
| **能力** | 能力列表与说明 | 习得时间与熟练度变化 | Lorebook: "擅长岩石魔法"；World Engine: `skills: [{name: "岩石魔法", learned: "60年", level: 5}]` |
| **目标** | 长期目标或动机 | 当前具体目标与完成状态 | Lorebook: "想要复仇"；World Engine: `goals: [{desc: "找到仇人线索", status: "in_progress"}]` |
| **事件历史** | 关键背景事件摘要 | 完整事件时间线 | Lorebook: "曾被封印300年"；World Engine: `events: [{time: "188年", text: "被封印"}, {time: "488年", text: "被解封"}]` |
| **身份** | 稳定身份标签 | 身份获得/失去的时间 | Lorebook: "凤凰骑士团成员"；World Engine: `events: [{time: "488年", text: "加入凤凰骑士团"}]` |

### 7.2 "初始年龄" vs "当前年龄"

正确做法：
- Lorebook 写**外貌年龄**或**初始设定**："薇洛丝外貌19岁"
- World Engine 记录**当前年龄**：`age: 19`（初始）→ 一年后自动或手动更新为 `age: 20`

错误做法：
- ❌ Lorebook 写"薇洛丝19岁"，但故事推进到第二年后忘记更新
- ❌ World Engine 和 Lorebook 都写"19岁"，导致同步问题

### 7.3 Lorebook 不需要同步更新的原则

**关键原则**：Lorebook 是**稳定设定仓库**，不需要随剧情实时同步。

适合写进 Lorebook 的：
- ✅ 角色的底层人格、价值观、说话方式
- ✅ 世界观规则（魔法体系、政治制度、地理）
- ✅ 稳定关系标签（"主角的导师"）
- ✅ 关键背景事件摘要（"曾被封印"）

不需要写进 Lorebook 的：
- ❌ 当前 HP / MP
- ❌ 当前位置
- ❌ 当前具体目标（"找到城主"）
- ❌ 每一次对话的内容
- ❌ 每一次状态变化

**何时更新 Lorebook**：
1. 角色的核心设定发生**永久性变化**（如"失去魔法能力"、"加入新组织"）
2. 世界观规则需要**补充或修正**
3. 长篇连载中，**每卷结束后**整理一次稳定设定

不要每写一章就更新一次 Lorebook。World Engine 已经记录了所有动态变化，Lorebook 只保存"长期稳定、反复引用"的设定。

## 8. 各系统职责边界

写作模式下，World Engine 与 Lorebook、Manuscript 三者职责互补、不重叠：

| 系统 | 职责 | 存什么 | 不存什么 |
| --- | --- | --- | --- |
| **World Engine** | 动态世界状态、时间线 | 随时间演变的状态与事件：谁在哪、HP 多少、何时发生了什么 | 静态世界观、最终正文 |
| **Lorebook** | 静态设定、世界观、规则 | 长期稳定的设定：身份、力量体系、地理、势力关系、规则 | 随剧情变化的瞬时状态、时间线 |
| **Manuscript** | 最终正文 | 读者会读到的章节 prose | 结构化状态、设定卡 |

Lorebook 与 World Engine 的互补关系最容易混淆，用一个例子点清楚：

- Lorebook 存**身份设定**："艾莉娜是凤凰骑士团成员"——这是稳定 canon，不随单章剧情改变。
- World Engine 存**时间线事件**："艾莉娜在 488 年加入骑士团"——这是发生在某个 instant 上、可被 reduce 的状态变化。

判断一条信息归谁：**它会不会随剧情时间演变？** 会演变（位置、HP、心理、当前关系、谁知道了什么）进 World Engine；不会演变的稳定 canon（人物底设、世界规则、地理）进 Lorebook。稳定下来的事实可以从 World Engine 整理回 Lorebook，但两者不互相替代。

> 边界提醒：World Engine 第一版**不接旧 simulation workflow**，也**不依赖 Plot 系统**。在写作模式的提示词层面，把这两个系统当做不存在，不要引导读者去用 plot / simulation 工具维护世界状态。

## 9. 技术细节透明原则（原 §3）

**用户只需要讲故事、设计角色、推进剧情，永远不需要理解 World Engine 的技术结构。** leader 负责把用户的自然语言自动映射到 slice / patch / reduce / instant / op / schema / calendar 这些技术概念，用户侧不应该看到它们。

时间是这条原则最硬的约束：**对用户一律用项目日历字符串**（如 "公元2020年4月12日 18:00"）。HTTP 公开入参禁止 raw instant；`execute_world` 沙箱内使用 instant bigint，leader 通过 `world.time.parse()` / `world.time.format()` 做转换，用户和 writer 只看到日历文本。

应该对用户说的"人话" vs 不该暴露的术语对照：

| 应该用的人话 | 不该暴露的术语 |
| --- | --- |
| "薇洛丝现在在星陨遗迹，HP 80/100" | "reduce subject `weiluosi` 得到 `location=ref(...)`, `hp=80`" |
| "我把这段剧情记到时间线里了" | "写入了 4 个 slice，每个含若干 patch" |
| "我帮你补一段莉雅的过去" | "向更早 instant 插入一个 `kind=backstory` 的 slice" |
| "公元2020年4月12日 18:00" | "instant: 63722311200" |
| "薇洛丝学会了岩石魔法" | "对 `/skills` 做 `append` patch" |
| "她现在状态怎么样" | "execute_world 查询状态" |

leader 在回复用户时用左列，在调用工具时用右列。报告世界状态时给"时间线 + 当前状态"的人读摘要，不要把 slice id、patch JSON、op 名字甩给用户。

## 10. 写作模式整体流程（原 §4）

下面是写作模式的宏观流程原理，不是固定步骤清单——实际顺序随用户而变，关键是理解每个阶段在做什么、World Engine 在什么时机进入。

1. **灵感探索**：用户抛出题材、主角、大致走向。leader 确认理解、读取已导入的 lorebook、提出几个关键问题，用自然对话收敛方向。**此阶段不要急着初始化 World Engine、不写 Plot、不写正文。**
2. **项目初始化**：方向清晰后，建立项目骨架（lorebook 设定、人物卡等）。
3. **World Engine 初始化（合适时机引入）**：见下方时机判断。
4. **世界观扩展**：补充人物、地点、势力、规则。结构化设定进 lorebook，需要追踪状态的对象登记为 subject。
5. **剧情设计**：设计章节剧情，把状态变化推进进 World Engine（见第 5、6 节）。
6. **正文写作**：writer 基于设计好的世界状态写 prose（见第 6 节）。

**World Engine 初始化的时机判断**：

- 当项目有**明确时间线**、且有**需要追踪状态的角色**时引入。这通常出现在用户从"探索想法"转向"我要正经写这个故事了"的时刻，或用户明确说"建立 World Engine"。
- **纯灵感探索阶段不引入**。用户还在反复推翻设定、连主角身份都没定时，初始化 World Engine 只会制造需要反复推翻的结构化数据，得不偿失。

初始化时 leader 需要和用户确认：用什么纪年（Calendar）、故事"现在"是哪个时间点、开局要追踪哪些角色。然后建立 world subject 作为纪元锚点、登记初始角色、写入开局状态。这些技术动作对用户表现为"确认几个基础信息 → 世界引擎建好了 + 当前状态摘要"。

## 11. 两种剧情录入模式（原 §5）

World Engine 同时支持两种把剧情录入时间线的模式，两者可以混用：

**模式 A：先设计世界 / 状态，再写剧情（结构化）**

用户给出结构化设计要求（"设计主角薇洛丝和配角莉雅"）。leader 引导补全关键设定，把人物卡写进 lorebook，把需追踪的状态登记为 subject 并写入背景 slice。适合开局前准备，设定完整、细节明确。

**模式 B：先给剧情叙述，再补回 World Engine（自然）**

用户直接讲一段剧情（"薇洛丝转生到无名祭坛，遭遇邪教徒逃亡，进入遗迹见到被封印的莉雅……"）。leader 从自然语言里**提取时间、地点、事件、状态变化**，补齐缺失的关键信息（"大约第几天？遗迹叫什么？"），再按时间顺序写入若干 slice。适合剧情推进中，快速、自然。

两种模式的差异：

| 维度 | 模式 A：先设计 | 模式 B：先叙述 |
| --- | --- | --- |
| 用户输入 | 结构化设计要求 | 自然剧情叙述 |
| leader 行为 | 引导设计 → 确认细节 → 写入 | 提取信息 → 补齐细节 → 写入 |
| World Engine 侧重 | 补角色历史背景 | 记录剧情时间线事件 |
| Lorebook 侧重 | 创建人物卡 | 创建地点卡、补经历 |

无论哪种模式，录入都遵循**最少支持当前叙事**原则：群体角色先用单一 subject 表示整体（"邪教徒巡逻队"而不是逐个邪教徒），需要时再拆分重要个体；每个 subject 通常 1-2 条 slice（起因 + 当前状态）；临时龙套不建 subject，只在主角 slice 的事件文本里提及；背景按需补，不预先填满。

补设定走**向更早时间插入 slice**：剧情需要莉雅展现岩石魔法时，在她被封印之前的某个时间点插入一个 `kind=backstory` 的 slice 记录她何时学会魔法。可以用精确时间，也可以用一个代表时刻 + `summary` 文本表达模糊时间段（"2010-2015 年间跟随导师学习"）。

## 12. Leader-Writer 协作模式（原 §6）

这是写作模式的核心协作契约。writer 拥有 World Engine **只读**查询能力，因此协作流程围绕"写作前 leader 已经把世界状态推进好"展开。

### 6.1 标准流程（推荐）

```
leader 设计剧情 → leader 推进 World Engine（写作前完成）→ leader 准备简化 brief → writer 自主查询 World Engine 写正文 → leader 检查成果
```

关键原则是**先演化世界 + 设计剧情，然后再调用 writer**。leader 在调用 writer 之前，就把本章涉及的剧情事件按时间顺序写入 World Engine（解封、交流、追入、对峙……）。这样世界状态先行，writer 看到的永远是一致的、已推进到位的状态，而不是滞后于正文的状态。

### 6.2 Brief 简化原则（两条正交的轴）

brief 的"简化"其实是两条互相独立的轴，别混为一谈：

- **轴 A：可查询状态是否进 brief —— 由防全知模式决定。**
  - `autonomous`（自主全知，默认）：writer 有 World Engine 只读 + Plot 只读能力，可查询状态（HP / 位置 / 属性 / 关系）**不进 brief**，brief 只给「查哪些 subject、哪个时间窗」的查询提示。塞状态进 brief 既冗余，又让 writer 退化成纯执行者、浪费它的查询能力。
  - `curated`（受控投喂，当前 leader 手动使用）：writer 读不到设定源，可查询状态**必须由编译器 / leader 展开投喂**，规则相对 autonomous 恰好反转；leader 投喂前按 `mustHide` 删减。
- **轴 B：剧情精细度 —— 是一个用户可调的档位，与模式无关。**
  - 默认粗粒度：brief 给章节目标 + 关键剧情点（框架级）。
  - 精细档：可细到分镜级（用户拨到高精度时）。精细版本本轮不做，但档位属于 brief 内容本身，不受防全知模式影响。

两轴的共同不变量（任何模式、任何精细度都成立）：

| brief 始终传 | brief 始终不传 |
| --- | --- |
| 章节目标 / 关键剧情点 | 设定复述（角色底设、力量体系、世界规则 → 指向 lorebook） |
| 信息控制要求（读者已知 / 主角已知 / 必须隐藏 / 可暗示） | 文风约束（文风、避讳词、节奏方言 → 全在 writer profile） |
| 本章覆盖参数（仅覆盖 writer 默认的项，如 POV） | 完整时间线 / patch 细节 |
| 建议读取（由 Scene refs 编译，带 relation gloss） | —— |

可查询状态（HP / 位置等）落在轴 A：autonomous 不传（writer 自查），curated 传（编译器代查代填）。brief 格式契约的逐段定义见 [../plot/writer-brief.md](../plot/writer-brief.md)。

### 6.3 Writer 能力边界

writer 当前默认 `autonomous` 模式，能力如下（`curated` 模式会剥掉 World Engine / Plot / bash，只留受控投喂 + 反问 leader 逃脱口，当前由 leader 手动切换）：

| 能力 | 说明 |
| --- | --- |
| 查询 World Engine | `execute_world`（writer 绑定 readonly 模式） |
| 查询 Plot（只读） | `get_chapter_writer_brief` / `get_story_chapter` / `get_story_scene_context` 等；只读，不创建 / 修改 Thread / Scene / Chapter |
| 读取 lorebook | 角色设定、地点描述、规则 |
| 自主查询状态 | 按需查角色 HP、位置、心理等 |
| 写作自由度 | 可在 brief 框架内发挥细节 |
| 写入 World Engine | **不能**，不可写入切面 |
| 创建 subject | **不能**，首次写入由 leader 负责 |
| 主线剧情设计 | **不应承担**，剧情设计权在 leader |

writer 的典型工作流（autonomous）：有 `input.chapterId` 时用 `get_chapter_writer_brief` 自取本章 brief → 读 brief 指定的 lorebook → 用 readonly `execute_world` 查相关 subject 在章节时间范围的状态 → 构思并写入正文 → 报告结果。leader 在 writer 完成后检查成果，确认 writer 的细节发挥（环境描写、角色反应、内心独白等）是否在合理范围内；通常这些细节不改变世界状态，不需要补回 World Engine。

### 6.4 自由发挥模式（可选，默认不推荐）

```
leader 给大致方向 → writer 自由发挥（含剧情细节）→ leader 事后把偏离 brief 的状态变化补回 World Engine
```

当**用户明确允许 writer 自由发挥**时使用：leader 只给方向（"薇洛丝和莉雅遭遇邪教徒发生战斗，战斗细节你自由发挥"），writer 可以增加新角色、改变受伤程度、使用未预设的能力。写作后 leader 读取正文、提取状态变化，把它们补进 World Engine（创建新角色 subject、更新 HP、补能力溯源）。

此模式文字生成快、即兴感强，但 writer 承担了部分剧情设计职责、World Engine 滞后于正文、需要更多后处理，因此**默认不推荐**，只在探索性 / 实验性写作且用户明确同意时使用。需要严格控制剧情走向时不要用。

## 13. 信息控制（原 §7）

leader 在 brief 中必须明确**谁知道什么、谁不知道什么**，writer 严格按角色视角写，不泄露角色不该知道的设定。

信息控制写进 brief 的形式是按 subject 视角分别说明知识边界，例如：

- 薇洛丝视角：不知道莉雅的真实身份、被封印的原因、项链的意义。
- 莉雅视角：失忆，不知道自己被封印了多久、不知道外面的世界。
- 反派视角：从教会典籍见过古代魔女项链的记载，认出标志，但不确定眼前的女孩是谁。

writer 写每个角色的言行、心理时只能基于该角色已知的信息。读者层面的悬念由"角色不知道、读者也暂时不知道"共同维持。注意 World Engine 的 readonly `execute_world` 是上帝视角真值源——writer 能查到莉雅的真实状态，但在薇洛丝视角的叙述里不能让薇洛丝"知道"这些 writer 查到的设定。查询能力服务于写作一致性，不等于授权角色越界知情。

## 相关文档

- [README.md](README.md)：World Engine reference 书架入口。
- [recording-principles.md](recording-principles.md)：最少支持当前叙事原则——记录什么、记录到什么粒度。
- [schema-system.md](schema-system.md)：schema、kind、op、ref、default 契约。
- [subject-lifecycle.md](subject-lifecycle.md)：subject 注册、切面演化、reduce、issues、查询契约。
- [calendar-system.md](calendar-system.md)：Instant、纪元锚点、Calendar 与时间入参边界。
- [`.agents/tasks/56-world-engine/README.md`](../../../.agents/tasks/56-world-engine/README.md)：World Engine 模型与 Decisions 定论（契约真相源）。
