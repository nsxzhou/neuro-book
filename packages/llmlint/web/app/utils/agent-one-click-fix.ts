/** “一键修到底”固定结果目标；独立于 Vue composable，供行为测试与多个入口复用。 */
export const ONE_CLICK_FIX_INSTRUCTION = `对当前全文执行一次面向 AI 痕迹风险的整体润色。

当前草稿中的安全机械修复已在启动前应用；请从 current 工作副本读取更新后的正文，不要再次尝试机械修复。

先理解文本的叙事信息、段落功能、人物声音和上下文，再决定修改范围。不要机械地逐条替换所有规则：
- 标记为强判别的规则必须修复。
- 标记为 AI 敏感词的规则必须修复；结合语境自行选择自然、准确的表达，不做生硬同义词替换。
- 标记为弱判别的规则酌情修复；只有确实形成机器腔、套话或拖累表达时才改。
- 证据不足、无判别力或反向规则只作参考，不为清零而改坏正文。

可以自由选择、重复或组合调用 read、get_revision_detections、lint_check、edit 等工具，没有固定调用顺序。结合逐检测器热力图和规则密度定位 AI 风险高的句段；必要时可以重写完整句子或整段，在不改变事实、情节、视角、人物意图和有效声音的前提下完成小范围润色。

处理高风险句段时，在内部先生成多个都符合原意与语境的候选，排除语义、语法和人物声音不合格的选项后，选择其中你最不会优先选择、最不像模型惯用措辞和结构的一个。不要把候选列表写入正文或聊天，只通过 edit 提交最终选择。

完成必修项并处理值得修改的弱判别风险后调用 finish。summary 概括主要润色区域，以及有意保留的弱判别信号。`;
